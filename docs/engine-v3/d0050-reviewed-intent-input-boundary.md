# D-0050 — Reviewed observations and current intent bridge

Source: `6056f31fee26df468dac8d8d81b37a889afec77c` on
`codex/evaluation-d0036-d0041`. Evaluation-only; no policy or public change.
This public report contains no real observations, feedback labels or private hashes.

## Actual result and scope

The authorized private preparation was reproduced from the confirmed journal,
its archived code and source bindings. Original preparation and feedback remain
separate and unchanged. The new pure pre-adapter verifies those bindings again,
retains every field/UNKNOWN wrapper and produces a field-to-target/evidence map.
It **does not produce an executable Hotel DTO**, call a ranking engine, write a
journal, perform custody or infer a human judgment. Private results remain outside
Git. A blocked faithful input is not an observed engine abstention or agreement.

## Boundary findings, not transcription-critical heuristics

| Finding | Actual code rule | Consequence / minimum next work |
|---|---|---|
| Strong distance preference already exists | `intentRolePolicyBridgeV3.assertContext/prepare`: explicit `strong-preference`, `reference=selected-location`, positive distance and selected point; outside preference is contextual, not a hard violation | Reuse this policy. Source-centre text alone cannot prove the selected point or geographic equivalence. Preserve the numeric observation separately; require verified common reference/coordinates or a separately specified source-reference boundary. No invented origin, blanket tolerance or exception copied from feedback. |
| Unknown complete total | `prepare`: policy total only for `reported-complete`; `personalUtilityRolePolicyV3` comparable gate requires known total | Not a universal input parse failure. The policy can calculate experience from partial evidence but cannot recommend those incomplete candidates. A future faithful all-incomplete execution may abstain; this phase does not claim it occurred. |
| Unknown rating scale | V2 quality expects canonical 0–10; the pre-adapter omits an unnormalizable observation | Keep the original value and unknown scale; do not assume a scale or invent normalized evidence. This alone is not a universal execution blocker. |
| Observed public availability | `HotelOffer.bookable` is a required boolean; reliability rejects no-bookable-offer; compatibility `createSingleSolution` requires bookable for feasible | Observation proves neither verified true nor known false. A false placeholder would manufacture a different case. A diagnostic tri-state offer contract or explicitly authorized verification is still needed. |
| Children and sleeping places | `SmartStayEngineV2SearchInput` carries children count, not ages; comfort mandatory requirements use unit/feature codes, not sleeping-place adequacy | Preserve exact ages, requested unit count, beds and scope in the projection. Do not equate guest capacity with verified beds or set hard constraints satisfied. Add an explicit offer-level adequacy/unknown boundary before faithful execution. Some offers additionally lack relevant bed/exclusivity evidence. |
| Exact room/rate | `resolveEvaluatedOfferV3` binds the actually evaluated offer | Keep selected room, price and cancellation references together. Another rate in the gallery remains context; never borrow its conditions, collapse rates or invent an additional fully transcribed alternative. |
| Roles | Current policy separates Choice/Saving/Upgrade | Best Choice may eventually be compared alone; a human over-budget role remains unmapped, not an Upgrade or exception authorization. Two post-observation contexts remain one diagnostic case. |

`scripts/reviewed-intent-input-assessment-v1.mjs::assessReviewedIntentInput`
accepts the confirmed packet/events, reviewed code hash, original scenario
manifest and prior preparation with exact hashes. It has no feedback argument.
Its output is labelled `NON_EXECUTABLE_REVIEWED_FIELD_PROJECTION`, with
`engineInput=null`, evidence/boundary blockers and recommendation-only findings
reported separately. It is intentionally a **source-specific assessment**, not
a general canonical serializer with a hidden fallback. Passing its integrity
checks does not grant eligibility. Do not pass the projection to the engine.

Unknown amounts, tax statements, deposits, cancellation timezones, room evidence
and counts are retained without coercion. Numeric unit conversion of explicit
source-centre distances does not change their reference. No source order,
advertising or feedback choice becomes a decision feature. Current Balanced,
maximum-distance, comfort, Upgrade and privacy policies remain byte-identical.

## Verification and delivery

Twelve focused regressions cover journal/input integrity, actual bridge rejection
of an absent distance origin, child/bed contract limits, unknowns, offer binding,
feedback separation and no accidental execution. These are invented fixtures,
not transcriptions of private offers. Canonical validation is performed against
an isolated committed candidate excluding the seven developer paths, with the
seventeen D-0037 sealed files unchanged. Exact counts and exit codes are recorded
in the phase delivery; an unexecuted dependency-registry audit is not release CI.

The private delivery contains the full five-offer matrix, all field/proof links,
missingness, preserved journal/material hashes and separate feedback. Nothing in
that private output is committed. No new review, collection, real engine run,
custody, Golden claim, public activation or main publication occurs.

## Minimum next step

Specify the evaluation-only offer-scope/availability boundary (including child
ages, sleeping-place requirements and UNKNOWN) and source-reference distance
semantics before assembling real canonical inputs. Resolve only the actual
missing evidence identified per offer; do not re-review unchanged transcription.
Existing selected-location strong-preference policy and price eligibility must
not be weakened. No future real execution command is supplied while the input is
not faithful. Complete-price proof remains necessary for recommendation, but its
absence must not be misreported as the reason the bridge cannot parse any input.

Every software delivery reports local/remote SHA, push status and still-local
commits. Work-branch-only synchronization requires exact remote verification.
