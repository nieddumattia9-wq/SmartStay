# D-0068 — Room presentation and cancellation instants

Date: 2026-09-19. Approver: Mattia, second audit lot R02/R06.
Source: `f03b83d0b403e1b8bd82c92606d9855df7133db2`.

## Context and decision

Presentation syntax is not accommodation evidence, and timestamp spelling is
not a commercial change. Interpret a bounded set of ordinary HTML presentation
without executing it, while retaining exact originals and qualification traces.
Explicit valid offsets may identify the same cancellation instant; uncertain
local times do not acquire an invented zone.

Room text, structured beds, sleeping sufficiency and ancillary conditions remain
separate. Supported presentation changes must preserve negations, conditions,
OR alternatives and unknown places. Uncertainty about an identifiable ancillary
subject is not automatically a restriction on the base beds. Unsupported or
ambiguous bed content remains unresolved. No quantities are inferred from
capacity, room names, HTML attributes or external resources.

The cancellation repair affects semantic comparison, not raw storage or offer
identity. Other commercial fields, amounts, penalties and policy text retain
their checks. Only documented timestamp fields with valid explicit syntax can
participate in instant equivalence. Calendar errors, missing zones and unknown
offsets must not silently normalize. No change to refund rules or ranking.

## Safeguards and alternatives

Reject wholesale HTML stripping, executable rendering, arbitrary natural-language
date parsing, implicit local zones and rounding distinct instants together.
Do not loosen source authentication or migrate historical acquisitions,
inventories, progress or authorizations. This is a forward software correction;
the historical evidence stays bound to its original code.

R07, A01 and A02 remain outside scope. Main, weights, recommendation roles,
protected developer work and sealed D-0037 files remain untouched. No provider
calls, private decryption, acquisition, credential, real engine run or deployment.
Public cancellation comparison can change a false change warning, but this does
not promote V3 or alter any recommendation policy.

## Validation and implementation consequences

Preserve synthetic counterexamples before correction. Check equivalence,
actual differences and insufficient information through the affected callers,
then official Windows V2/V3 and lifecycle gates, TypeScript/build and the exact
release commands required by D-0035 in a clean isolated committed candidate.
Local results are not GitHub CI. Measured evidence and remaining grammar limits
are recorded in `../../engine-v3/d0068-room-temporal-equivalence-validation.md`.

Selective work-branch commit and non-force publication are authorized only after
the required gates pass. Report actual remote readback and pending commits.
If rollback is later authorized, use a forward commit; never rewrite history or
rebind an old custody inventory. Supersedes only the two defective interpretations,
not any historical result, pricing rule or commercial authorization.
